Здравствуйте. Сегодня мы поговорим с
вами о моделировании случайных величин с
заданным законом распределения.
Перед началом лекции давайте немного
вспомним пройденное ранее.
исходным материалом для формирования в
цифровых устройствах случайных величин с
различными законами распределения,
как правило, служит равномерно
распределённые на интервале от нуля до
одного псевдослучайные числа,
генерируемые либо специальным физическим
генератором,
либо программным способом.
При этом программный способ реализации
приводит к тому, что получаемые числа,
строго говоря, нельзя назвать
случайными, так как при каждой
реализации алгоритма
будут получаться, если не предпринимать
дополнительных операций, идентичные
последовательности чисел.
Поэтому, как мы уже обсуждали ранее,
числа, получаемые при использовании
программных генераторов, называют
псевдослучайными,
понимая под этим термином тот факт, что
статистические свойства
последовательности таких чисел близки к
статистическим свойствам истинных
случайных последовательностей.
Степень близости или, наоборот,
заметного отклонения устанавливается с
помощью специальных статистических
инструментов.
В нашем случае это были графические
тесты.
Однако число существующих законов
распределения достаточно велико и явно
не ограничивается ранее пройденными
нормальным и равномерными законами.
Поэтому возникают задачи моделирования
псевдослучайных чисел с заданным законом
распределения.
Для решения различных задач
статистического моделирования
существует необходимость получения
последовательностей
выборочных значений случайной величины с
заданным распределением.
Случайные величины обычно моделируют с
помощью преобразований.
одного или нескольких независимых
случайных величин, равномерно
распределённы на интервале от нуля до
единицы величины.
Поговорим об одном из наиболее часто
используемых методов моделирования
случайных величин с заданным законом
распределения.
Метод обратной функции распределения
основан на следующем свойстве
интегральной функции распределения.
Если плотность вероятности f малая
окситы от y непрерывной случайной
величины к удовлетворяет уравнению 1 или
что эквивалентно
функция распределения f большо сит от y
удовлетворяет уравнению 2,
где r малая - это случайная величина
равномерно распределённая на интервале
от ну до о, то случайная величина
распределена по закону f малая ксита от
y.
Обратим внимание, что для непрерывной
случайной величины к функция
распределения в большое ксито от y
является строго монотонной функцией, а
также неразрывной,
что обеспечивает существование обратной.
Для доказательства справедливости этого
свойства
сформируем на основе случайной величины
и её произвольного значения Yую
величину
1 равно интеграл от минус бесконечности
до X f макси от Y по d
и значение Yпе равное интегралу от минус
бесконечности до Y f малое кситы от y по
d, которое равно fе кто от y,
являющиеся монотонными функциями своих
верхних пределах интегрирования.
Тогда с учётом уравнения один из
предыдущего слайда имеем соотношение.
Вероятность, что X меньше Y равна
вероятности, что X
1 меньше, чем Y1 и равна вероятности
того, что равномерно распределённо на
интервале от нуля до одного величина
меньше, чем f большой окситы от Y и
равно
функции распределения
равномерно распределённой случайной
величины.
от аргумента
fксита большое от y.
Как известно, интегральная функция f
большое
от RТ
равномерного распределения на интервале
от нуля до одного
а
равняется нулю, если k меньше единицы,
равняется k, если мы лежим в диапазоне
от нуля до одного и равна единице, если
k больше одного.
То есть интегральная функция
распределения случайной величины к
действительно равна желаемой функции
f(x) от.
Для большинства практических интересных
модельных распределений
уравнение один из предыдущего слайда
оказывается трансцендентным.
Трансцендентное уравнение - это
уравнение, не являющееся алгебраическим,
то есть содержащее, например,
логарифмическое, тригонометрическое
или другое тождество.
И без учёта специфических свойств
рассматриваемого распределения трудно
указать общеэффективные методы его
решения. Использование численных
методов, например, метода Ньютона, может
привести к медленно сходящейся процедуре
на тех участках, где f мало к сито от y
мало по своей величине.
Ещё более сложной может оказаться
задача, если интеграл в уравнении 1 не
вычисляется в элементарных функциях, и
приходится применять либо аппроксимацию,
либо численное интегрирование.
Тем не менее, для некоторых простых
распределений удаётся получить результат
в замкнутом виде. Рассмотрим несколько
примеров.
Рассмотрим алгоритм моделирования
экспоненциального закона распределения.
Пусть дана плотность вероятности
случайной величины к с экспоненциальным
законом распределения.
По известной связи функции распределения
и плотности распределения вычислим
формулу первой.
Соответственно, fкси от y равно единице
минус экспонента в степени минулямбда
уже на y. При y равному нулю и функция
равна нулю в случае, когда y меньше 0.
Тогда значения этой функции
распределения
лежат в интервале от нуля до одного,
так же как и значения равномерно
распределённой случайной величины на
интервале от нуля до оного. Используем
метод обратной функции и приравняем эти
значения.
Как показано на первой формуле справа,
путём решения уравнения относительно к
получаем алгоритм моделирования
экспоненциально распределённой случайной
величины к
получим 5.000 псевдослучайных чисел,
используя полученный алгоритм
моделирования.
Установим константу лямбда равная
единице.
Оценим гистограмму полученной генерации
псевдослучайных чисел.
Как нетрудно заметить, она совпадает с
теоретическими.
Аналогичный результат получаем, сравнив
значение теоретической и эмпирической
функции распределения.
Как видно на слайде, значения оценок
математического ожидания и дисперсии
также близки к теоретическим.
В качестве следующего примера рассмотрим
моделирование распределения лапласа или,
как его ещё называют, двойного
экспоненциального распределения.
Поскольку у нас есть граничное значение
f большое от бета, алгоритм
моделирования распадается на две части.
Рассмотрим отдельно каждую часть, решаем
соответствующие уравнения, как показано
на слайде.
Установим константы альфа и бета, равные
единице и двойки. Соответственно,
смоделируем, используя полученный
алгоритм, 5.000 псевдослучайных чисел.
Нетрудно заметить сходство гистограммы и
плотности распределения с указанными
параметрами.
Аналогичный результат, получаемый при
сравнении эмпирической и теоретической
функции распределения.
Оценки математического ожидания и
дисперсии также близки к теоретическим.
Поговорим об ограничениях метода
обратной функции.
Пусть стоит задача смоделировать
бетарапределение
с указанное на слайде плотностью
распределения.
Чтобы использовать метод обратной
функции, нам необходимо брать сложный
интеграл от плотности распределения.
для определения функции распределения,
а затем ещё и непосредственно брать
интеграл от бетафункции.
Указано второй на слайде.
Можно, конечно, но для таких случаев
пользуются другим методом.
Рассмотрим его далее.
Смотрим метод, позволяющий реализовать
бетараспределение,
метод исключения или метод режекции,
который обычно связывают с именем Джона
Фонмана.
Пусть G маленькая, двумерная область, с
одной стороны ограниченная некоторым
интервалом от А до B, оси абсцис, а с
другой графиком функции f малояксита от
Y, являющейся плотностью распределения
моделируемой случайной величины к
поместим область G малая внутрь другой
области G большое. На рисунке
G - это прямоугольная область, как это
обычно бывает на практике. Y абсциссы
точек, Z ординаты точек, также
опирающиеся на интервал от А до B. И
пусть точка Y Z - это реализация
случайного двумерного вектора
Тта, равномерно распределённого в
области G.
большое.
Тогда процедура состоит в том, что при
выполнении неравенства f к от Y
больше либо равное z, значение y
принимается в качестве реализации
случайной величины с заданным законом
распределения f кситая малая от y, то
есть поддаётся на выход генератора
чисел. А в противном случае точка YZ
исключается из рассмотрения.
Далее процедура вновь возвращается к
реализации случайной точки с
координатами YZ и проверки неравенства
f мая ксита от Y больше либо равно z.
Другими словами, искомый набор чисел
составляет те точки области gя, которые
одновременно оказываются и в области g
малое. Так, точка с координатами YZ
первая, принимается в качестве
реализации, а точка с координатами YZ
вторая отбрасывается, поскольку не
попадает в область G малая.
Алгоритм, который мы обсудили на
предыдущем слайде,
графически можно представить, как
показано на слайде.
Вернёмся к нашему бетараспределению.
Пусть мы хотим получить 5.000
псевдослучайных чисел с
бетараспределением при помощи метода
исключения.
Параметры альфа и бета равны двум.
Сравним плотность распределения при
заданных параметрах и гистограмму.
Видим сходство.
Аналогичный результат получаемый для
функции распределения.
Параметры математического ожидания
дисперсии. также близки к теоретическим.
Оценим эффективность данного метода.
Как мы видим, при моделировании 5.000
точек почти 2.500 нам пришлось
отбросить.
То есть суммарно мы сгенерировали почти
7.500 точек вместо 5.000. Таким образом,
эффективность метода исключений тем
выше, чем больше вероятность
неисключения сгенерированной случайной
точки, которая определяется отношением Q
к площади под кривой F мало к сита от Y
к площади, охватывающей область G
большое.
Тогда вероятность того, что при
kдовательных шагах сгенерированная точка
не удовлетворяла бы условиям и
исключалась, а на k п шаге была бы
принята равна q ум x на 1 ми q в степени
k. Следовательно, среднее время
моделирования по методу исключения
одного числа пропорционально в величине,
представленной справа на слайде.
Рассмотрим ещё один метод моделирования.
Метод суперпозиции не имеет
самостоятельного значения, но может
оказаться весьма полезным
вспомогательным инструментом при
реализации других методов.
Его суть заключается в том, что
моделируемое распределение f малой
окситы от y представляется в виде
адтивной смеси других более простых
распределений.
Как показано
в первой формуле на слайде,
где от P первого до пктого - это
неотрицательные числа, весовые
множители, сумма которых равна единице.
Геометрически
формула под номером два означает, что
вся область под кривой f малой окситы от
y разбивается на несколько
непересекающихся подобластей.
GQТ,
где,
прошу прощения, GКТ,
где K равно от единицы до K большое,
площади которых равны
PКТ, как на рисунке.
Тогда распределение f мак от y
образуется посредством горизонтального
выравнивания под областей GQТ
и нормированием делением на PКТ ординат
точек F маяк Kт от Y.
Принцип разбиения заключается в том, что
области, имеющие большие площади, то
есть большие вероятности ПК, должны
соответствовать тем плотностям F малая
КТ от Yy, которые для своих реализаций
требуют малое количество ресурсов.
Соответственно, ресурсоёмким
распределением можно приписать малые
вероятности, и в среднем затраты будут
невелики, поскольку большая часть
времени будет отводиться на реализацию
распределений с малыми ресурсными
затратами.
Далее, каждое распределение реализуется
каким-либо известным способом.
Например, на рисунке показано разбиение
на ступенчатую плотность
F. малое пер от y прямоугольную область
f маше от y набор треугольных плотностей
f2
y f3y
f4 от f5 от f7 от и f8 от и остаточную
плотность f9 от которую можно вычислить
так как показано на слайде
ступенчатая прямо Угольная плотности
легко реализуется на основе
масштабирования
равномерного распределения.
Остаточную плотность F9 отY
подобной формы можно реализовать методом
исключений.
Для реализации треугольных плотностей
вида
F
к оксида от Y равно 2y, когда y
принадлежит интервалу от нуля до одного
и 0, когда y не принадлежит интервалу от
нуля до одного. Проще всего использовать
метод обратной функции, поскольку в этом
случае уравнение один, показанное на
слайде в начале презентации,
даёт нам
формулу
рав R, где случайная величина R
равномерно распределена на интервале от
нуля до одного.